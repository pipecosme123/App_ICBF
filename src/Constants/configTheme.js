import { createTheme } from "@mui/material";

const theme = createTheme({
   typography: {
      fontFamily: "Regular"
   },
   // palette: {
   //    primary: {
   //      main: '#0052cc',
   //    }
   // }
})

export default theme;